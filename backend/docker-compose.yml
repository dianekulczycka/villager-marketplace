services:
  db:
    image: mysql:8
    environment:
      - MYSQL_DATABASE=villager-marketplace-db
      - MYSQL_USER=user
      - MYSQL_PASSWORD=user
      - MYSQL_ROOT_PASSWORD=rootroot
    ports:
      - "3306:3306"